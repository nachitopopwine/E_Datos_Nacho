
    //////////ABSTRACCION/////////////// TALLER TIENE QUE COMPILAR EN CODESPACE
/*
Abstraccion , encapsulamiento , herencia , polimorfismo(sobrecarga y herencia).
Clase, objeto, metodo, atributos.

ENCAPSULACION: 
    private: nadie puede acceder a todo lo que esta dentro de esa clase solo dentro del contexto de la clase en especifico
    public: puede ser accedido dentro de el programa en general dentro de la clase que lo posee, sus clases derivadas y los objetos que lo instancien
    protected: solo puede ser accedido solo por la clase o las clases que la heredan

POLIMORFISMO SOBRECARGA:
    class A {
        public:
            void AFunc(int x);{
                //print x
            }
            void AFunc(double x);{
                //print x
            }
    }  

DATO:
"this" es un puntero por lo que se usa = this-> edad = edad;    
//para instanciar clase como puntero
    Persona* p1 = new Persona();
    p1-> getEdad();
    p1->SetEdad(23);









*/ 